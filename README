Dependencies are managed in requirements.txt
1. To install the packages, create virtual environment if not created yet:

    Run in terminal inside the project repo: 
    
        python -m venv .venv
    
    if created, activate it:
    - Mac:

            source .venv/bin/activate

    - Windows: 
        
            call .venv\Scripts\activate

2. Orchestrate the packages:
    - If not installed, run 
    
            *pip install -r requirements.txt*
            
    - If new packages are used: add them to requirements.txt 
    